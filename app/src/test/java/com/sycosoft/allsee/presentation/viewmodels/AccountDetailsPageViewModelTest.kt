package com.sycosoft.allsee.presentation.viewmodels

import kotlinx.coroutines.ExperimentalCoroutinesApi

class AccountDetailsPageViewModelTest {
    /*
    private val getPersonUseCase = mockk<GetPersonUseCase>(relaxed = true)
    private val getAccountsUseCase = mockk<GetAccountsUseCase>(relaxed = true)
    private val getAccountDetailsUseCase = mockk<GetAccountDetailsUseCase>(relaxed = true)

    private lateinit var underTest: AccountDetailsPageViewModel

    private val testScope = TestScope()

    @Before
    fun setup() {
        underTest = AccountDetailsPageViewModel(
            getAccountsUseCase = getAccountsUseCase,
            getPersonUseCase = getPersonUseCase,
            getAccountDetailsUseCase = getAccountDetailsUseCase
        )
        Dispatchers.setMain(UnconfinedTestDispatcher(testScope.testScheduler))
    }

    @After
    fun tearDown() {
        Dispatchers.resetMain()
    }

    @Test
    fun whenViewStateInitiallyEmits_ThenValueShouldBeLoading() = runTest {
        assert(true)
    }
     */
    // TODO: Ask how best to test this class. Throwing errors.
}